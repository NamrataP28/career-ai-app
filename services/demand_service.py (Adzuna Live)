import requests
import streamlit as st

def fetch_live_demand(role, country):
    ...
    companies = []

    for job in jobs:
        company = job.get("employer_name")
        if company:
            companies.append(company)

    top_companies = list(set(companies))[:10]

    return len(jobs), jobs, top_companies
